---
"fuelrats.com": patch
---

### Fixed
- On the dispatch board, the floating user-menu avatar no longer overlaps the notification and board-settings buttons when the site is installed as a home-screen app (standalone PWA, e.g. on iOS).
