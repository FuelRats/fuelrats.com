---
"fuelrats.com": patch
---

### Changed
- On the dispatch board, the tooltip showing a quote's exact timestamp can now be reached anywhere in the time column of that quote, instead of only on the few pixels the relative time ("3 hours ago") itself covers.
