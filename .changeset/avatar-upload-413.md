---
"fuelrats.com": patch
---

### Fixed
- Avatar uploads that are rejected by the server as too large (HTTP 413) now show a clear "avatar is too large" message instead of a generic internal-server error telling you to contact support.
